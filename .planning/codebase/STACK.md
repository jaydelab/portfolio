# Technology Stack

**Analysis Date:** 2026-03-25

## Languages

**Primary:**
- TypeScript - Frontend app entrypoint and UI code in `src/main.tsx`, `app/App.tsx`, `app/generated/Portfolio2026_1_86.tsx`, and `app/components/sections/*.tsx`
- CSS - Global styling and Tailwind v4 CSS-first setup in `styles/index.css`, `styles/tailwind.css`, `styles/theme.css`, `styles/responsive.css`, and `styles/visual-ir.css`

**Secondary:**
- JavaScript (ES modules) - Visual regression tooling in `tests/visual-baseline/capture.mjs` and `tests/visual-baseline/compare.mjs`
- JavaScript (Node CLI) - Bundled design-system/Figma helper project in `figma-cli/src/index.js` and `figma-cli/src/daemon.js`
- HTML - Single-page shell in `index.html`

## Runtime

**Environment:**
- Node.js `v24.13.0` detected locally via `node -v`
- Browser runtime for the shipped app, mounted from `index.html` into `src/main.tsx`

**Package Manager:**
- pnpm `10.30.3` detected locally via `pnpm -v`
- Lockfile: present in `pnpm-lock.yaml`

## Frameworks

**Core:**
- Vite `6.3.5` - Dev server and bundler configured in `vite.config.ts`
- React `18.3.1` - UI runtime used by `src/main.tsx` and all files in `app/`
- Tailwind CSS `4.1.12` - CSS-first utility framework configured in `styles/tailwind.css`

**Testing:**
- Playwright `1.58.2` - Screenshot capture in `tests/visual-baseline/capture.mjs`
- pixelmatch `7.1.0` - Pixel diff in `tests/visual-baseline/compare.mjs`
- pngjs `7.0.0` - PNG parsing for diff generation in `tests/visual-baseline/compare.mjs`

**Build/Dev:**
- `@vitejs/plugin-react` `4.7.0` - React integration in `vite.config.ts`
- `@tailwindcss/vite` `4.1.12` - Tailwind Vite plugin in `vite.config.ts`
- Framer Motion `12.38.0` - Motion effects in `app/components/effects/*.tsx`
- Three `0.183.2` - 3D dependency declared in `package.json`; custom shader/effect code exists in `app/components/effects/shader-animation.tsx`

## Key Dependencies

**Critical:**
- `react` `18.3.1` - Component rendering and app mount in `src/main.tsx`
- `react-dom` `18.3.1` - Root creation in `src/main.tsx`
- `vite` `6.3.5` - Local dev/build workflow from `package.json`
- `tailwindcss` `4.1.12` - Breakpoints and utility generation in `styles/tailwind.css`

**Infrastructure:**
- `playwright` `1.58.2` - Required by the project's visual-baseline workflow in `tests/visual-baseline/capture.mjs`
- `pixelmatch` `7.1.0` - Required by screenshot comparison in `tests/visual-baseline/compare.mjs`
- `framer-motion` `12.38.0` - Used for motion-ready section effects in `app/components/effects/card-entrance.tsx`, `app/components/effects/reveal-on-mount.tsx`, `app/components/effects/splash-intro.tsx`, and `app/components/effects/hero-title-reveal.tsx`

**Bundled tooling subproject:**
- `figma-ds-cli` `1.1.1` - Separate Node CLI packaged in `figma-cli/package.json`
- `ws` `^8.16.0` - WebSocket transport for the Figma helper daemon in `figma-cli/src/daemon.js`
- `commander` `^12.0.0` - CLI argument parsing in `figma-cli/package.json`
- `chalk` `^5.3.0` and `ora` `^8.0.0` - CLI UX in `figma-cli/package.json`

## Configuration

**Environment:**
- No `.env`, `.env.*`, or `*.env` files detected at repository root or one level below
- The shipped frontend app does not read `import.meta.env`; no frontend env contract was detected in `src/`, `app/`, or `styles/`
- Visual tooling accepts one optional process env var: `DEV_URL` in `tests/visual-baseline/capture.mjs`
- The bundled `figma-cli` subproject reads process env vars for local daemon/CDP behavior in `figma-cli/src/index.js` and `figma-cli/src/daemon.js`

**Build:**
- Vite config lives in `vite.config.ts`
- Tailwind v4 is configured directly in CSS via `styles/tailwind.css`
- Global CSS entrypoint is `styles/index.css`
- HTML shell is `index.html`
- Main package manifest is `package.json`
- Subproject manifest is `figma-cli/package.json`

## Platform Requirements

**Development:**
- Node.js and pnpm are required to run `vite` from `package.json`
- A modern browser is required for `ResizeObserver`, CSS transforms, and font loading used in `app/App.tsx`
- Playwright browsers are required for visual regression capture from `tests/visual-baseline/capture.mjs`
- If the bundled `figma-cli` tooling is used, Node `>=18` is required per `figma-cli/package.json`, plus a local Figma Desktop/CDP-capable setup referenced by `figma-cli/src/figma-client.js`

**Production:**
- Static frontend output generated by `vite build`
- Deployment target is not explicitly configured; no `vercel.json`, `netlify.toml`, `render.yaml`, `Dockerfile`, or CI/deploy config was detected in the repo root

---

*Stack analysis: 2026-03-25*
